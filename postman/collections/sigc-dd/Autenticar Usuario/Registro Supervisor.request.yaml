$kind: http-request
url: http://localhost:3000/api/auth/register
method: POST
body:
  type: json
  content: |-
    {
        "username": "{{SUPERVISOR_USERNAME}}",
        "password": "{{SUPERVISOR_PASSWORD}}",
        "estado": 1,
        "id_rol": 2
    }
order: 2000
